import { Box } from "@mui/material";
import { loginStyles } from "./Login.styles";

export const Login = () => {
	return (
		<Box
			sx={loginStyles.root}
		>
			sd
		</Box>
	);
};

export default Login;
